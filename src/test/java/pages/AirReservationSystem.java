package pages;

import java.util.ArrayList;

public class AirReservationSystem {
/*
    private  Scanner scanner;
    private  int input;
    private final static int QUIT = -1;
    private  boolean isValidInput;
    private  boolean[] seatsAvailability;
    private  SecureRandom numberRandom;
    private  int counterSeatsFirstClass;
    private  int counterSeatsEconomicalClass;
    private  int firstClassSeatsLenght;
    private  int constraintTries;
    private  boolean isFullInFirstClass;
    private  boolean isFullInEconomicalClass;



    public AirReservationSystem(SecureRandom numberRandom, int isValidInput) {

        ArrayList<Integer> lista;
        this.numberRandom = numberRandom;
        this.scanner = new Scanner(System.in);
        this.isValidInput = isValidInput;
        this.firstClassSeatsLenght = seatsAvailability.length % 2 == 0 ? seatsAvailability.length / 2 : (seatsAvailability.length / 2) + 1;


        AirReservationSystem.constraintTries = (constraintTries == 0 ? AirReservationSystem.constraintTries = 0: AirReservationSystem.constraintTries++);


        if( constraintTries == 0 )  AirReservationSystem.constraintTries = 0;
        else AirReservationSystem.constraintTries++;


        if( constraintTries == 0 ){
            AirReservationSystem.constraintTries = 0;
        }else{
            AirReservationSystem.constraintTries++;
        }



    }



*/
}
